<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weather_app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".di.WeatherApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_my_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_my_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Weatherapp">
        <activity android:name=".views.CitySelectionActivity" />
        <activity android:name=".views.WeatherForecastActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>